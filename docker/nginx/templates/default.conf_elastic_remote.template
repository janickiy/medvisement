# полный доступ к эластику по паролю
server {
    listen       9100;

    auth_basic "Restricted Content";
    auth_basic_user_file /etc/nginx/auth/.htpasswd;

    location / {
      proxy_pass http://elasticsearch:9200;
      proxy_redirect off;
    }
}